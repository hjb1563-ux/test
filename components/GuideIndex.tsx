'use client';
import {useMemo,useState} from 'react';
import Link from 'next/link';
import {ArrowRight,Search} from 'lucide-react';
import {guides} from '../data/guides/catalog';
export default function GuideIndex(){const [query,setQuery]=useState('');const found=useMemo(()=>guides.filter(g=>`${g.title} ${g.oneLine} ${g.keywords.join(' ')}`.toLowerCase().includes(query.toLowerCase())),[query]);return <><section className="guideHero"><div><span>BEGINNER RENOVATION GUIDE</span><h1>욕실 리모델링,<br/><em>알고 선택하세요</em></h1><p>철거부터 타일, 세면대, 변기, 욕조까지. 어려운 용어를 쉽게 정리했습니다.</p></div><div className="guideSearch"><Search size={19}/><input value={query} onChange={event=>setQuery(event.target.value)} placeholder="욕실 용어를 검색해보세요"/><small>예: 젠다이, 덧방, 구배, 언더볼, SMC</small></div></section><section className="guideCatalog"><div className="catalogTop"><div><span>17 TOPICS</span><h2>처음부터 끝까지, 순서대로 살펴보세요.</h2></div><Link href="/guide/glossary" className="secondary">용어 사전 보기 <ArrowRight size={15}/></Link></div><div className="guideCards">{found.map(guide=><article className="guideCardPro" key={guide.slug}><img loading="lazy" src={guide.image} alt=""/><div className="guideCardBody"><span className="guideOrder">{String(guide.order).padStart(2,'0')}</span><h3>{guide.title}</h3><p>{guide.oneLine}</p><div className="guideMetrics"><span>현장 확인 <b>{'★'.repeat(guide.difficulty)}{'☆'.repeat(5-guide.difficulty)}</b></span></div><Link href={`/guide/${guide.slug}`}>가이드 보기 <ArrowRight size={15}/></Link></div></article>)}</div>{found.length===0&&<p className="emptyGuide">검색 결과가 없습니다. 다른 용어로 검색해 보세요.</p>}</section></>}
